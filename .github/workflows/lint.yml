name: lint
on:
  pull_request:
    paths:
      - "**/*.ts"
      - "**/*.md"
  push:
    paths:
      - "**/*.ts"
      - "**/*.md"

jobs:
  cspell:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: npx cspell "**/{*.ts,*.md}"
