"use strict"

const fs = require('fs');
const commander = require('commander');

export const help = `
This is some help.
`;

/**
 * run code
 */
export function command(cmd) {
  const prompt = "Enter your name:";

  return prompt;
}

command(commander);
