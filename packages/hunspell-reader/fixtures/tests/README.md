# Test Fixtures

This directory contains test fixtures.

Each fixture contains multiple files:

- `<name>.aff` - aff file
- `<name>.dic` - dictionary
- `<name>.good.txt` - these words should be found in the dictionary
- `<name>.wrong.txt` - these words should NOT be found in the dictionary.
