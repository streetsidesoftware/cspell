# yaml-language-server: $schema=./../../cspell-tools.config.schema.json

targets:
  - name: color-cities-code
    sources:
      - ../dicts/colors.txt
      - filename: ../dicts/cities.txt
        split: true
        keepRawCase: true
      # - filename: ../dicts/sampleCodeDic.txt
      #   keepRawCase: true
    format: plaintext
