# yaml-language-server: $schema=./../../cspell-tools.config.schema.json

targets:
  - name: colors
    targetDirectory: ../../temp/builds/build-single-target-yaml
    sources:
      - ../dicts/colors.txt
    format: plaintext
