// import { Grammar, Pattern, PatternMatch } from './grammarDefinition';
// import { DocumentParser } from './parser';

// interface LineOffset {
//     text: string;
//     offset: number;
// }

// class DocumentParserFromGrammar implements DocumentParser {
//     constructor(readonly grammar: Grammar) {}
//     parse(line: string): ParseLineResult {}
// }

// export function createDocumentParser(grammar: Grammar): DocumentParser {
//     return new DocumentParserFromGrammar(grammar);
// }

// function matchPattern(pattern: Pattern, line: LineOffset): RegExpExecArray | undefined {}

// function matchPatternMatch(p: PatternMatch, line: LineOffset): RegExpExecArray | undefined {}
