import { CSpellIO } from 'cspell-io';

/**
 * The settings controller manages requests to resolve configuration settings and files.
 */
export class SettingsController {
    constructor(readonly cspellIO: CSpellIO) {}
}
